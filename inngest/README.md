# Inngest jobs

Background + scheduled work. Wired in Phase 3 (OCR extract) and Phase 4
(reminder dispatch cron, every 5 min). Kept empty in Phase 1 by design — no
job definitions yet, so no Inngest client/endpoint is registered.
